{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build & run",
            "type": "shell",
            "command": "docker compose up --build --wait",
            "group": "build"
        }
    ]
}